# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data handling
pandas==2.2.0
python-dotenv==1.0.1

# HTTP client (for calling LLM APIs)
httpx==0.27.0

# Optional: if you want to use sentence-transformers later (for vector DB upgrade)
# sentence-transformers==3.0.1
# chromadb==0.5.0

# For PDF parsing (if you later support product sheets in PDF)
# PyMuPDF==1.24.0